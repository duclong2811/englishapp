import{describe,expect,it}from"vitest";import{a2Unit2,a2Unit3}from"./a2-units-2-3";describe("A2 Unit 2 và 3",()=>{it.each([a2Unit2,a2Unit3])("có đủ nội dung học",unit=>{expect(unit.lessons).toHaveLength(5);for(const lesson of unit.lessons){expect(lesson.vocabulary.length).toBeGreaterThanOrEqual(8);expect(lesson.examples.length).toBeGreaterThanOrEqual(15);expect(lesson.mastery.length).toBeGreaterThanOrEqual(4);expect(lesson.canDo?.length).toBeGreaterThan(0);expect(lesson.spiralReview?.length).toBeGreaterThan(0);expect(lesson.accentExposure).toEqual(["en-US","en-GB"])}})});
