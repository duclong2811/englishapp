"use client";import{useEffect,useState}from"react";import{useLessonLanguage}from"@/components/LessonLanguageProvider";import{RecordingStorageSettings}from"@/components/RecordingStorageSettings";import type{AssistMode}from"@/lib/assist/registry";import type{ExplanationMode}from"@/lib/curriculum/bilingual-schema";
type Prefs={englishLevel:string;koreanLevel:string;dailyMinutes:number;vietnameseSupport:boolean;assistMode:AssistMode;explanationMode:ExplanationMode;accent:"US"|"UK"};const initial:Prefs={englishLevel:"A1",koreanLevel:"beginner",dailyMinutes:20,vietnameseSupport:true,assistMode:"off",explanationMode:"both",accent:"US"};
export default function SettingsPage(){const[prefs,setPrefs]=useState(initial);const[saved,setSaved]=useState(false);const{setMode}=useLessonLanguage();useEffect(()=>{fetch("/api/state").then(r=>r.json()).then(data=>{const p=data.preferences;if(p)setPrefs({...initial,...p,explanationMode:["both","vi","ko"].includes(p.explanationMode)?p.explanationMode:"both"})}).catch(()=>{})},[]);async function save(){await fetch("/api/state",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type:"preferences",...prefs})});setMode(prefs.explanationMode);setSaved(true);setTimeout(()=>setSaved(false),1800)}return <div className="page narrow"><header className="page-head"><div><span className="eyebrow">CÀI ĐẶT</span><h1>Cài đặt học tập</h1><p>Điều chỉnh trình độ tiếng Anh và cách hiển thị giải thích Việt–Hàn riêng biệt.</p></div></header><section className="settings-section"><h2>Trình độ và ngôn ngữ</h2><div className="grid-2"><div className="field"><label htmlFor="english">Trình độ tiếng Anh</label><select id="english" value={prefs.englishLevel} onChange={e=>setPrefs({...prefs,englishLevel:e.target.value})}><option>A1</option><option>A2</option><option disabled>B1 — bản đồ đã sẵn sàng</option><option disabled>B2 — bản đồ đã sẵn sàng</option></select></div><div className="field"><label htmlFor="explanation">Ngôn ngữ giải thích</label><select id="explanation" value={prefs.explanationMode} onChange={e=>{const explanationMode=e.target.value as ExplanationMode;setPrefs({...prefs,explanationMode});setMode(explanationMode)}}><option value="both">Tiếng Việt + tiếng Hàn</option><option value="vi">Chỉ tiếng Việt</option><option value="ko">Chỉ tiếng Hàn</option></select><small>Nội dung tiếng Anh mục tiêu luôn hiển thị.</small></div></div></section><section className="settings-section"><h2>Thói quen học</h2><div className="grid-2"><div className="field"><label htmlFor="duration">Thời gian học mỗi ngày</label><select id="duration" value={prefs.dailyMinutes} onChange={e=>setPrefs({...prefs,dailyMinutes:Number(e.target.value)})}><option value="10">10 phút</option><option value="20">20 phút</option><option value="30">30 phút</option></select></div><div className="field"><label htmlFor="accent">Giọng phát âm mặc định</label><select id="accent" value={prefs.accent} onChange={e=>setPrefs({...prefs,accent:e.target.value as"US"|"UK"})}><option>US</option><option>UK</option></select></div></div></section><section className="settings-section"><h2>Dữ liệu của tôi</h2><RecordingStorageSettings/><p style={{fontSize:13,color:"var(--muted)"}}>Bản JSON chỉ chứa metadata của bản ghi, không chứa tệp âm thanh lớn.</p><div className="actions"><a className="secondary" href="/api/export">Xuất dữ liệu học tập</a><button type="button" className="secondary" disabled>Nhập dữ liệu — đang phát triển</button></div></section><div className="actions"><button type="button" className="primary" onClick={save}>Lưu cài đặt</button>{saved&&<span className="save-status" role="status">Đã lưu.</span>}</div></div>}
