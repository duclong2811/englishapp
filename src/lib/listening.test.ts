import{describe,expect,it}from"vitest";import{normalizeDictation,scoreDictation}from"./listening";
describe("dictation",()=>{it("ignores harmless case and punctuation",()=>{expect(normalizeDictation("  I’m HERE! ")).toBe("im here");expect(scoreDictation("I am here.","i am HERE!").correct).toBe(true)});it("accepts documented variants",()=>{expect(scoreDictation("I am here.","I'm here",["I'm here."]).correct).toBe(true)});it("keeps meaningful vocabulary differences",()=>{const result=scoreDictation("I take the bus.","I take the train.");expect(result.correct).toBe(false);expect(result.meaningfulErrors).toContain("bus")})});
