import{describe,expect,it}from"vitest";import{calculateMastery,scoreExercise,weakSkills}from"./exercise";import type{Exercise}from"./curriculum/schema";
const base={id:"x",prompt:"",skill:"grammar",objectiveId:"o"}as const;
describe("exercise scoring",()=>{it("normalizes punctuation and case for objective answers",()=>{const e={...base,type:"fill-blank",answer:"I am here."}as Exercise;expect(scoreExercise(e," i AM here ").correct).toBe(true)});it("scores arrangements deterministically",()=>{const e={...base,type:"arrange",answer:["Nice","to","meet","you"]}as Exercise;expect(scoreExercise(e,["Nice","to","meet","you"]).score).toBe(1)});it("never pretends to grade production",()=>{const e={...base,type:"written",modelAnswer:"Hello"}as Exercise;expect(scoreExercise(e,"anything")).toEqual({scorable:false,correct:null,score:null})})});
describe("mastery",()=>{it("keeps separate skill scores and detects weak areas",()=>{const scores=calculateMastery([{skill:"vocabulary",score:1},{skill:"vocabulary",score:.5},{skill:"grammar",score:1}]);expect(scores.vocabulary).toBe(75);expect(scores.grammar).toBe(100);expect(weakSkills(scores)).toContain("listening")})});
