import{beforeEach,describe,expect,it,vi}from"vitest";import{BrowserSpeechProvider,classifyVoice,discoverVoices,selectVoice}from"./browser-speech";
const voice=(lang:string,name=lang)=>({lang,name,voiceURI:name,localService:true,default:false} as SpeechSynthesisVoice);
describe("browser speech",()=>{beforeEach(()=>vi.stubGlobal("SpeechSynthesisUtterance",class{lang="";voice:SpeechSynthesisVoice|null=null;rate=1;constructor(public text:string){}}));it("classifies only supported locale metadata",()=>{expect(classifyVoice("en-US")).toBe("en-US");expect(classifyVoice("en_GB")).toBe("en-GB");expect(classifyVoice("en-AU")).toBeNull()});it("discovers and selects preferred locale voices",()=>{const voices=[voice("en-US","us"),voice("en-GB","uk"),voice("ko-KR","ko")];expect(discoverVoices({getVoices:()=>voices})["en-GB"][0].name).toBe("uk");expect(selectVoice(voices,"en-US")?.name).toBe("us")});it("reports unsupported and missing-locale fallback honestly",()=>{expect(new BrowserSpeechProvider(undefined).speak("hello")).toEqual({ok:false,error:"unsupported"});const synth={getVoices:()=>[voice("ko-KR")],cancel:vi.fn(),speak:vi.fn(),pause:vi.fn(),resume:vi.fn()} as unknown as SpeechSynthesis;expect(new BrowserSpeechProvider(synth).speak("hello",{locale:"en-GB"})).toEqual({ok:false,error:"voice-unavailable"})})});
