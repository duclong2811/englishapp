export function normalizeDictation(value:string){return value.normalize("NFKC").toLowerCase().replace(/[“”‘’.,!?;:]/g,"").replace(/\s+/g," ").trim()}
export function scoreDictation(answer:string,response:string,acceptedVariants:string[]=[]){const normalized=normalizeDictation(response);const matches=[answer,...acceptedVariants].some(item=>normalizeDictation(item)===normalized);const expectedWords=normalizeDictation(answer).split(" ");const actualWords=normalized.split(" ");const missing=expectedWords.filter((word,index)=>actualWords[index]!==word);return{correct:matches,score:matches?1:Math.max(0,1-missing.length/expectedWords.length),meaningfulErrors:matches?[]:missing}}
